appName: merch-api

image:
  name: merch-api
  repository: jalgraves/merch-api
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

containerPort: 5000
ingressHost: merch-api.jalgraves.com

db:
  name: merch
  host: postgres.default.svc.cluster.local
  port: 5432

logLevel: INFO
ingress:
  enabled: false

domain:
api_pass:
api_user:
db_host:
db_pass:
db_port:
db_user:
db_name:
frontend_origin_url:
