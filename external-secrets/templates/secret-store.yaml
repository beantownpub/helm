apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-ssm
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.aws.region }}
      auth:
        jwt:
          serviceAccountRef:
            name: {{ .Release.Name}}
